name: Server A

on:
  push: 
    tags:
      #- '(v\d+\.\d+\.\d+|\d{8}-\d{4})'
      #- 'v[0-9]{8}-[0-9]{4}'
      - '*'
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Testing
        run: |
          echo "Building Server A for tag: ${{ github.ref_name }}"